(jbuild_version 1)

(executables
 ((names (mondrian))
  (libraries (vlayout cairo2))))

(rule
 ((targets (mondrian.pdf))
  (action (with-stdout-to ${@} (run ${test:mondrian.exe})))))

(alias
 ((name runtest)
  (deps    (mondrian.exe))
  (action (run ${<}))))
